{
  "name": "shopify-product-uploader",
  "version": "1.0.0",
  "description": "Bulk upload products to Shopify from a local CSV file using the Admin API",
  "main": "upload-products.js",
  "scripts": {
    "upload": "node upload-products.js",
    "dry-run": "node upload-products.js --dry-run",
    "test": "echo \"No tests configured yet\""
  },
  "keywords": ["shopify", "products", "bulk-upload", "csv", "admin-api"],
  "license": "MIT",
  "dependencies": {
    "@shopify/shopify-api": "^11.0.0",
    "csv-parser": "^3.0.0",
    "dotenv": "^16.4.0"
  }
}
